import React from 'react';

const HomeSidebar = ({sidebar, setSidebar}) => {
    return (
        <div className={sidebar ? 'left-0 ': '-left-full'}>
            
        </div>
    );
};

export default HomeSidebar;